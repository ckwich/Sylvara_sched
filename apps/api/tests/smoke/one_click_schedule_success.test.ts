import { describe, test } from 'vitest';

describe('A2 one-click schedule success', () => {
  test.todo('Create foreman roster + eligible job, call one-click schedule, and assert one ScheduleSegment + one SegmentRosterLink are created.');
  test.todo('Assert no END_OF_DAY travel is auto-created and START_OF_DAY exists only when explicitly requested.');
});
