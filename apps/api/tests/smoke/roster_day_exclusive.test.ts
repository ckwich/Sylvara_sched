import { describe, test } from 'vitest';

describe('A1 day-exclusive roster membership', () => {
  test.todo('Create two foreman rosters on the same local date and verify second crew assignment hits unique violation on (date, person_resource_id).');
  test.todo('Verify Crewman1 remains assigned only to the first roster after the rejected insert.');
});
