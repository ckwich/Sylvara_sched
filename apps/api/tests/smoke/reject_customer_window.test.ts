import { describe, test } from 'vitest';

describe('A6 reject customer window conflict', () => {
  test.todo('Configure customer window 10:00-14:00 and verify attempt outside window is rejected with CUSTOMER_WINDOW_CONFLICT.');
});
