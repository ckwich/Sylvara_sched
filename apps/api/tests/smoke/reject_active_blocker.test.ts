import { describe, test } from 'vitest';

describe('A5 reject active blocker', () => {
  test.todo('Create JobBlocker ACTIVE and verify scheduling attempt is rejected with ACTIVE_BLOCKER and no new segment.');
});
