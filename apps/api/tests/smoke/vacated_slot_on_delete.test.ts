import { describe, test } from 'vitest';

describe('A8 vacated slot on delete', () => {
  test.todo('Soft-delete a 09:00-13:00 segment and assert VacatedSlot(DELETED) OPEN with matching start/end.');
});
