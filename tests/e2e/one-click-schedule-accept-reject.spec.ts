import { test } from '@playwright/test';

test('B3 one-click schedule accept/reject', async () => {
  test.fixme(true, 'TODO: 1) run one-click schedule success path 2) verify onsite segment visible 3) verify reject messages for ACTIVE_BLOCKER, CUSTOMER_WINDOW_CONFLICT, and NO_CONTIGUOUS_SLOT_AT_CLICK');
});
