import { test } from '@playwright/test';

test('B4 close out day creates END_OF_DAY travel', async () => {
  test.fixme(true, 'TODO: 1) ensure at least one onsite segment exists 2) open Close out day 3) verify default start equals latest end 4) save and verify END_OF_DAY block + second-close-out behavior');
});
